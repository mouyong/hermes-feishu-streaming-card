# V4.6.x 增量体验实施状态

2026-09-20 授权实施，V4.6.4 已正式发布（PR #336，精确合并 `457f0d00ddedc0e4dc6a507cf79c4ddfc6aaa17e`）。本机已升级 4.6.4 并恢复 ready；初次测试被 DeepSeek HTTP 503 阻塞，后续桌面真实 A 选择与续答已观察成功；严格首击时序及手机未验证。后续候选为 **4.6.5**，继续按补丁版本交付，已有默认保持兼容。

4.6.5 已实现持久通知归属、已知原生通知来源、明确工具 call_id、可选时间线显示、模型报错去重及严格迁移；发布门禁见 [4.6.5 说明](../../release-notes-v4.6.5.md)。下表记录 4.6.4 已交付范围，未验证的真实客户端项仍保留。

## 目标与来源

让用户看见进展、首次点动选择、顺着聊天读到续答，并保留历史问题和实际结果。重点来源为 [#335](https://github.com/baileyh8/hermes-feishu-streaming-card/issues/335) 首次按钮与回复倒序、[PR #331](https://github.com/baileyh8/hermes-feishu-streaming-card/pull/331) 续答与通知方案、[#328](https://github.com/baileyh8/hermes-feishu-streaming-card/issues/328)/[#333](https://github.com/baileyh8/hermes-feishu-streaming-card/issues/333) 阅读取舍，以及 [#330](https://github.com/baileyh8/hermes-feishu-streaming-card/issues/330) 贡献者提交前验证。

#328/#333 的原开关已在 4.6.2/4.6.3 交付，本轮增加可选组合和解释工具。已有终局恢复、失败保文、话题路由和展示检查点是不可回归的底线，不重新包装为新功能。PR #331 按子项吸收，不整包合并；归因见[发布候选说明](../../release-notes-v4.6.4.md)。

## V4.6.4 已交付范围

| 工作项 | 当前实现 | 必须保持的边界 |
| --- | --- | --- |
| 冷启动首次按钮 | 首 turn/interaction eager 接线，覆盖真实生成的 ctx-only clarify/approval 闭包与实际 SDK processor | 精确 Gateway/source/profile/live adapter；保留 dispatcher identity；未知结构 fail-open |
| 按需续答 | clarify/approval 选择后只记录边界，有新输出才新建同路由续答；连续题无空白夹卡 | 问题/选择回执留存；一个当前显示 owner；不清空整轮历史、附件和统计 |
| 失败与恢复 | 新卡确认成功才切 owner；失败/不确定保留旧 owner，唯一 legacy owner 同方言更新与恢复 | 不逐 delta 重发，不对 legacy PATCH schema 2.0，不复活旧审批 |
| 阅读方式 | 可选 classic/focused/detailed，card-config 只读说明有效值和来源 | 缺省不变；同层显式字段优先；现有 true/false 终态工具语义不变 |
| 通知清理 | 已登记重启通知按 exact profile/bot/chat/thread 的成功投递快照清理 | 来源不明的原生 home 提示保留；删除失败不丢记录；答案/回执/失败说明保留 |
| 提交前验证 | 默认只读 preflight，显式 focused/full 真跑 pytest 与对应 diff base；缺 fixture 准确报告 | 私有测试 state/操作目标，返回真实退出码，分享摘要不含私密路径和原始日志 |

续答增量只显示边界后的新内容；上游完整终局快照以“本轮完整结果”保留，不猜前缀裁切。分段是有界显示投影，不是新的执行队列。代码契约见[交互续答](../../wiki/interaction-continuation.md)。

## 完成顺序与门禁

1. **候选实现及模块回归**：保留首次按钮、续答、作用域通知与旧 owner 方言的失败复现；运行相关 focused、实际生成 hook、SDK 和 HTTP/检查点回归。独立审阅发现的缺口必须补实执行用例。
2. **集成候选验证**：完整 pytest、diff 检查；固定 Hermes 安装/重复安装/还原；普通 wheel 来源和配置兼容；中断、终局恢复、容量保护与多 profile/topic 的组合验证。
3. **真实实例验收**：复用授权实例/会话，核对实际配置、包路径、版本、服务和身份；验证桌面首次点击与顺序阅读，手机/故障注入未运行的项目单独标注，不能由 mock 或旧版本实测替代。
4. **发布门禁**：精确合并 SHA 的 CI、annotated tag、三平台资产/checksum、公开 tag 普通安装；分别记录生产升级和真实客户端证据。发布与关闭 issue 以证据为准，不以时间窗口、测试数量或相邻修复替代。

4.6.4 的发布已完成；真实实例和客户端步骤的当次结果由[本轮验收清单](../../wiki/feishu-acceptance-v4.6.4.md)与发布交付记录确认，本文不预先勾选。

## 关键验收

- 冷启动第一题/第一条审批，不用 slash/model/resume 预热；一次点击只唤醒一次原等待方，生成闭包没有 self/runner 也能正确路由。
- 单选、多选、自定义答案、连续两题、拒绝、过期/重审；旧题 token 和旧审批不得作用于新题，编辑中不被无关 PATCH 清空。
- 选择前后的正文、问题、操作范围、工具、附件和整轮统计可回看；新段在选择之后，直接下一题无空卡。
- 新续答 create 失败/不确定、重复/晚到事件、旧 animation、终局与重启；只有确认新 owner 后切换。唯一 legacy owner 始终同方言，展示恢复不恢复执行。
- 精确 bot/profile/chat/topic、空 thread 与首回复建 thread；通知无法证明同一作用域时保留。失败删除、容量、迟到删除不得误撤回答案和回执。
- 缺省和旧 YAML 无变化；focused/detailed 与多层显式覆盖、completed/failed、长思考/表格/代码及回退一致。card-config 解释不是已重载证明。
- preflight 错误 cwd/解释器、缺 fixture、真实 pytest 非零和提交差异空白错误都如实报告，不接触生产 operations target。

真实验收清单需分别记录桌面与实际可用手机设备。无法验证的 #282 等历史现场问题继续保留边界；#335 的当前客户端行为要独立复验，不能仅凭贡献者此前实测或本轮自动化关闭。

## 本轮不扩大的范围

不自动改变阅读默认，不实施全量架构重写、第二套执行/授权队列、跨会话任务看板、全功能设置 UI 或长答案文件导出。AMD/9Router 运维、默认模型切换与 Hermes 核心升级属于独立任务。Obsidian 镜像由主任务按授权处理；本文不包含私有运行目标。

## 当日新增社区输入

- [PR #338](https://github.com/baileyh8/hermes-feishu-streaming-card/pull/338) 承接 #331 的通知和工具显示工作。4.6.5 以可选字段吸收时间线正序与每段成功工具限制，并以 producer/application 证明处理原生 home 通知；不接受默认值反转、不因普通文本匹配删除消息。背景任务成功提示和审批超时的独立计时撤回另行验证。
- [Issue #337](https://github.com/baileyh8/hermes-feishu-streaming-card/issues/337) / [PR #339](https://github.com/baileyh8/hermes-feishu-streaming-card/pull/339) 提议终态审批降噪与旧段收尾。旧段收尾已实际复现并纳入 4.6.5：保存旧内容与选择记录，跳过 legacy owner，采用中性转交状态。无条件隐藏已决定审批仍需独立回执送达证明，不将整个 #337/#339 宣称已解决。
- [Issue #340](https://github.com/baileyh8/hermes-feishu-streaming-card/issues/340) 报告过程面板工具序号/执行状态不可见。尚需复现截图中的客户端布局与配置；显式 call_id 去重不等于已经修复该视觉问题。
